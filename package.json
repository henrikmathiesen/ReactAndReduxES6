{
  "name": "reactAndReduxES6",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "http-server": "^0.9.0",
    "react-scripts": "0.8.4"
  },
  "dependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "serve": "npm run build && http-server ./build -p 1337",
    "deploy": "process.env.npm_package_homepage=\"http://henrikmathiesen.github.io/reactAndReduxES6\" && npm run build&&gh-pages -d build",
    "eject": "react-scripts eject"
  }
}
